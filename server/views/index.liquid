{% layout "server/layouts/base.liquid" %}

{% block content %}

<h1 class="h1">{{ title }}</h1>

<section class="pokemon">
  <ul>
    {% for newsItem in gameNewsData.appnews.newsitems %}
    <li>
      <a href="/news/{{ newsItem.gid }}">
        {{ newsItem.title }}
      </a>
    </li>
  {% endfor %}
  </ul>
  
</section>

{% endblock %}
